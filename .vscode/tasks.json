{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "PHPCS: Check staged files",
      "type": "shell",
      "command": "$files = git diff --cached --name-only --diff-filter=ACM | Where-Object { $_ -match '\\.(php|module|install|theme)$' }; if ($files) { $files | ForEach-Object { phpcs -q $_ } } else { Write-Host 'Not files to look' }",
      "problemMatcher": [],
      "group": "test"
    },
    {
      "label": "PHPCS: Check current file",
      "type": "shell",
      "command": "phpcs -q ${file}",
      "problemMatcher": [],
      "group": "test",
      "presentation": {
        "reveal": "always",
        "panel": "new"
      }
    },
    {
      "label": "PHPCBF: Auto-fix current file",
      "type": "shell",
      "command": "phpcbf -q ${file}",
      "problemMatcher": [],
      "group": "build"
    }
  ]
}
